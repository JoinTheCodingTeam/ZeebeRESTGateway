# Web UI for testing REST requests

Provides UI to validate REST request and create corresponding Zeebe XML.

## Usage
1. Install dependencies.
    ```
    npm install
    ```

2. Run development server:
    ```
    npm run dev
    ```
   
3. Build for production:
    ```
    npm run build 
    ```
